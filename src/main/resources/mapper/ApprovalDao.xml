<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="rise.cc.dao.ApprovalDao">
    <insert id="createApproval" parameterType="rise.cc.dto.Approval" useGeneratedKeys="true" keyProperty="docNo">
        INSERT INTO cc_appro (
            doc_type,
            emp_id,
            retention_period,
            doc_title,
            doc_content,
            create_dt,
            deadline_dt,
            draft_YN,
            total_line_count,
            status_line_count
        ) VALUES (
                     #{docType},
                     #{empId},
                     #{retentionPeriod},
                     #{docTitle},
                     #{docContent},
                     #{createDt},
                     #{deadlineDt},
                     #{draftYN},
                     #{totalLineCount},
                     #{statusLineCount}
                 )
    </insert>
    <update id="updateApproval" parameterType="rise.cc.dto.Approval">

    </update>
    <update id="deleteApproval" parameterType="rise.cc.dto.Approval">

    </update>
    <select id="getApproval" parameterType="rise.cc.dto.Approval">

    </select>
</mapper>