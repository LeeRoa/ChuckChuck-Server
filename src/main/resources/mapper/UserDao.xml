<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="rise.cc.dao.UserDao">
    <select id="getUser" parameterType="Map" resultType="rise.cc.dto.User">
        SELECT
            COUNT(*)
        FROM
            DUMMY
    </select>

</mapper>